# IPO Light

React frontend with an Express/MongoDB backend.

## Backend

```bash
cd server
npm install
npm run dev
```

Set real values in `server/.env` before using MongoDB Atlas or SMTP email delivery.
